import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:sqlite_todo/app/modules/home/pages/book_register/book_register_page.dart';

void main() {
  testWidgets('DogRegisterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DogRegisterPage(title: 'DogRegister')));
    //  final titleFinder = find.text('DogRegister');
    //  expect(titleFinder, findsOneWidget);
  });
}
