import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:sqlite_todo/app/modules/home/pages/book_register/book_register_controller.dart';
import 'package:sqlite_todo/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // DogRegisterController dogregister;
  //
  setUp(() {
    //     dogregister = HomeModule.to.get<DogRegisterController>();
  });

  group('DogRegisterController Test', () {
    //   test("First Test", () {
    //     expect(dogregister, isInstanceOf<DogRegisterController>());
    //   });

    //   test("Set Value", () {
    //     expect(dogregister.value, equals(0));
    //     dogregister.increment();
    //     expect(dogregister.value, equals(1));
    //   });
  });
}
